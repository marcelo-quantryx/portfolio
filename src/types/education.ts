export interface Education {
  institution: string;
  website: string;
  location: string;
  degree: string;
  period: string;
  description: string;
}

export interface Language {
  name: string;
  level: string;
}